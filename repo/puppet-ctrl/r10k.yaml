:cachedir: 'cache/r10k'
#:git:
#  private_key: "/etc/ssh-key/id_rsa"

:sources:
  # This will clone the git repository and instantiate an environment per
  # branch in /etc/puppet/code/environments
  :base:
    remote: 'https://github.com/cantaveneraf-cgi/puppet-repo.git'
    basedir: '/etc/puppet/code/environments'